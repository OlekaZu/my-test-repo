#include <stdio.h>

void sum(int a, int b) {
  int res = a + b;
  printf("%d\n", res);
}

void string(char arr[]) { printf("%s\n", arr); }

float mult(float a, float b) {
  float res = a * b;
  return res;
}